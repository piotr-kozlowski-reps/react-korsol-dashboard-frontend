# ważne info

instalowanie dependencies bez ich aktualizacji:
npm install --legacy-peer-deps

# backendDeploy

- commit
- push develop branch
  git checkout main
  git merge develop
  git push

# frontendDeploy

npm run build
